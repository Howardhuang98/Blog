# conda 虚拟环境

conda是一个功能强大的包管理器，虚拟环境管理器。

1. 查看所有虚拟环境

```shell
conda env list
```

2. 激活虚拟环境

```shell
activate env_name
```

3. 退出虚拟环境

```shell
deactivate env_name
```

4. 删除虚拟环境

```shell
conda env remove -n env_name --all
```

虚拟环境保存位置：

.\user\Anaconda3\envs\

